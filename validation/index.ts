import { area } from '@eigakan/validation/area';
import { cinema } from '@eigakan/validation/cinema';
import { cinemaName } from '@eigakan/validation/cinemaName';
import { code } from '@eigakan/validation/code';
import { email } from '@eigakan/validation/email';
import { movieTitle } from '@eigakan/validation/movieTitle';
import { reCaptchaToken } from '@eigakan/validation/reCaptchaToken';

const validation = {
  area,
  cinema,
  cinemaName,
  code,
  email,
  movieTitle,
  reCaptchaToken,
};

export { validation };
